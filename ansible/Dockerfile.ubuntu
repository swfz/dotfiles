FROM ubuntu:resolute@sha256:678c6550cc43645e08669028bc177f50be4e7c5b8cca677067b1914d4afc7a03

RUN apt-get update \
    export DEBIAN_FRONTEND=noninteractive \
    apt-get install -y software-properties-common gpg-agent curl sudo tzdata \
    add-apt-repository ppa:deadsnakes/ppa -y \
    apt-get update \
    apt-get install -y python3.12 python3.12-venv \
    curl -LsSf https://astral.sh/uv/install.sh | sh \
    mv ~/.local/bin/uv /usr/local/bin/uv \
    uv venv --python python3.12 \
    uv pip install --no-cache ansible ansible-lint

WORKDIR /app

