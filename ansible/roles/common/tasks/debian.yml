- name: install apt-packages
  apt: name={{ item }} state=present
  with_items:
    - curl
    - bash-completion
    - gcc
    - make
    - tar
    - zsh
    - unzip
    - dos2unix
    - openssl
    - ctags
    - nkf
    - libssl-dev
    - libreadline-dev
    - zlib1g-dev

