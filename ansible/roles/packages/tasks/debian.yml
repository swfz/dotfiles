---
- name: Install apt-packages
  ansible.builtin.apt:
    name:
      - curl
      - bash-completion
      - gcc
      - make
      - tar
      - zsh
      - unzip
      - dos2unix
      - openssl
      # TODO 一旦除外、エラー対応して戻す
      # - ctags
      - nkf
      - libssl-dev
      - zlib1g-dev
      - libffi-dev
      - autogen
      - automake
      - libperl-dev
      - libbz2-dev
      - zlib1g-dev
      - imagemagick
      - ubuntu-wsl
      - gnuplot
      - sqlite3
      - libsqlite3-dev
      - inotify-tools
      - translate-shell
      - silversearcher-ag
    state: present
